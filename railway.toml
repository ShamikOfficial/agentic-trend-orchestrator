[build]
builder = "NIXPACKS"

[deploy]
startCommand = "alembic upgrade head && uvicorn backend.app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
